extends layout

block content
    h1= title
    h3 active-users
    input#input_userAppID(type="hidden",name="appID",value= userAppID)
    ul#list-active-users
        //handle active-users
        //if(list_rooms != null)
        //    each val in list_rooms
        //        li
        //            a(href= "/room/" + val.roomID)= val.username
    h3 contacts
    ul#list-contacts
        //handle contact
block script
    //script(src="/room/minion/socket.io/socket.io.js")
    script(src="/socket.io/socket.io.js")
    //script(src="https://cdn.socket.io/socket.io-1.4.5.js")
    script(ssrc="/javascripts/contact-active-users.js")